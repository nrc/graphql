[package]
name = "trilogy"
version = "0.1.0"
authors = ["Nick Cameron <ncameron@mozilla.com>"]

[dependencies]
env_logger = "0.4"
graphql = { path = "../../graphql" }
graphql-macros = { path = "../../graphql-macros", features=["rustfmt"] }
